const { createInstallments } = require("./installments");
const { createExternalTransaction } = require("./externalTransactions");

module.exports = { createInstallments, createExternalTransaction };
