const newBookingRequest = require("./newBookingRequest");
const getUserBookings = require("./getUserBookings");
const viewBooking = require("./viewBooking");

module.exports = { newBookingRequest, getUserBookings, viewBooking };
